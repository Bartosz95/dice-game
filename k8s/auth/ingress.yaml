apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minimal-ingress
spec:
  rules:
  - host: "bartekapp.pl"
    http:
      paths:
      - path: "/"
        pathType: Prefix
        backend:
          service:
            name: demo
            port:
              number: 80