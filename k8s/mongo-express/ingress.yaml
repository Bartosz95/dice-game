apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dice-game-mongo
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  tls:
    - hosts:
      - dice-game-mongo
  rules:
  - host: dice-game-mongo
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: mongo-express
            port:
              number: 80